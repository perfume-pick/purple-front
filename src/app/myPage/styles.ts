import { theme } from "@/styles/theme";
import styled from "@emotion/styled";

const Wrapper = styled.div`
  padding: 0 1.6rem 9.7rem;
  background-color: ${theme.color.white};
`;

export const S = {
  Wrapper,
};
